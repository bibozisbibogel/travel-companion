# <!-- Powered by BMAD™ Core -->

# Story 2.2: Hotel Agent & Accommodation Search

## Status
Draft

## Story
**As a** travel planner,
**I want** the system to find suitable accommodation options,
**so that** I can choose hotels that match my preferences and budget.

## Acceptance Criteria
1. Hotel Agent implemented with Booking.com API integration
2. Hotel search with location, check-in/out dates, guest count, and budget filters
3. Hotel data model including amenities, ratings, location, and pricing
4. Integration with multiple hotel APIs (Expedia, Airbnb as fallbacks)
5. Hotel ranking algorithm considering price, location, and ratings
6. Cache mechanism for hotel search results

## Tasks / Subtasks
- [x] Hotel Agent Base Implementation (AC: 1)
  - [x] Create HotelAgent class in `packages/api/src/travel_companion/agents/hotel_agent.py`
  - [x] Inherit from base agent class with configuration and dependency injection
  - [x] Add agent initialization with hotel-specific configurations
  - [x] Create unit tests for base hotel agent functionality

- [x] Booking.com API Integration Service (AC: 1, 2)
  - [x] Create Booking.com API client in `packages/api/src/travel_companion/services/external_apis/booking.py`
  - [x] Implement XML API authentication with credentials
  - [x] Add hotel availability search integration with `/json/bookings.getHotelAvailabilityV2`
  - [x] Implement proper rate limit handling (100 requests/minute per property type)
  - [x] Create unit tests for API client with httpx-mock

- [x] Hotel Data Models (AC: 3)
  - [x] Create Pydantic models in `packages/api/src/travel_companion/models/external.py`
  - [x] Implement HotelOption model matching database schema
  - [x] Add HotelSearchRequest and HotelSearchResponse models
  - [x] Create model validation with proper type hints including amenities JSONB
  - [x] Add unit tests for model validation and serialization

- [x] Hotel Search Logic (AC: 2)
  - [x] Implement search_hotels method in HotelAgent
  - [x] Add location, check-in/out dates, guest count, and budget filter parameters
  - [x] Integrate with Booking.com API service
  - [x] Add result caching with Redis to optimize API usage
  - [x] Create comprehensive unit tests for search functionality

- [ ] Multiple API Integration and Fallbacks (AC: 4)
  - [ ] Create Expedia API client in `packages/api/src/travel_companion/services/external_apis/expedia.py`
  - [ ] Create Airbnb API client in `packages/api/src/travel_companion/services/external_apis/airbnb.py`
  - [ ] Implement API fallback chain: Booking.com → Expedia → Airbnb
  - [ ] Add circuit breaker pattern for external API calls
  - [ ] Create error handling tests with mock failures

- [ ] Hotel Ranking and Comparison (AC: 5)
  - [ ] Implement hotel ranking algorithm considering price, location proximity, and ratings
  - [ ] Add weighted scoring system for hotel options with preference matching
  - [ ] Create hotel filtering methods by budget, amenities, and location distance
  - [ ] Add sorting and pagination for large result sets
  - [ ] Create unit tests for ranking and comparison logic

- [ ] Cache Mechanism Implementation (AC: 6)
  - [ ] Implement Redis caching strategy for hotel search results with TTL
  - [ ] Add cache invalidation logic for outdated hotel availability
  - [ ] Create cache key strategies based on search parameters
  - [ ] Implement cache warming for popular destinations
  - [ ] Add performance tests for caching effectiveness

## Dev Notes

### Previous Story Insights
From Story 2.1 (Flight Agent):
- Base agent pattern established in `packages/api/src/travel_companion/agents/base.py`
- External API service pattern established in `packages/api/src/travel_companion/services/external_apis/`
- Circuit breaker and rate limiting patterns defined for external API calls
- Redis caching layer configured and available for API response caching
- Pydantic models in `packages/api/src/travel_companion/models/external.py` for external API responses
- Test infrastructure established with pytest, httpx-mock, and Docker containers

### Tech Stack Requirements
[Source: architecture/tech-stack.md]
- **Python**: 3.11+ for backend development with async support, LangGraph compatibility
- **FastAPI**: 0.104+ for API orchestration with high performance, excellent OpenAPI docs
- **httpx**: 0.25+ for async API calls - modern async HTTP client for Python
- **Pydantic**: 2.5+ for data models & validation with runtime type checking, excellent FastAPI integration
- **pytest**: 7.4+ for backend testing with industry standard, excellent fixture support
- **Redis**: 7.2+ for API caching & rate limiting with high performance

### Project Structure and File Locations
[Source: architecture/source-tree.md]
**Hotel Agent Implementation Structure**:
- **Base Agent**: `packages/api/src/travel_companion/agents/base.py` (already created from Story 2.1)
- **Hotel Agent**: `packages/api/src/travel_companion/agents/hotel_agent.py` (to be created)
- **Booking.com API Client**: `packages/api/src/travel_companion/services/external_apis/booking.py` (to be created)
- **Expedia API Client**: `packages/api/src/travel_companion/services/external_apis/expedia.py` (to be created)
- **Airbnb API Client**: `packages/api/src/travel_companion/services/external_apis/airbnb.py` (to be created)
- **External API Models**: `packages/api/src/travel_companion/models/external.py` (extend existing)
- **Agent Tests**: `packages/api/src/tests/test_agents/test_hotel_agent.py` (to be created)
- **API Client Tests**: `packages/api/src/tests/test_services/test_booking.py` (to be created)
- **Test Fixtures**: `packages/api/src/tests/fixtures/hotel_data.json` (to be created)

### Booking.com API Integration Requirements
[Source: architecture/external-apis.md]
**Booking.com API Configuration**:
- **Base URL**: `https://distribution-xml.booking.com/`
- **Authentication**: XML API with credentials
- **Rate Limits**: 100 requests/minute per property type
- **Primary Endpoint**: `POST /json/bookings.getHotelAvailabilityV2` for hotel availability search
- **Secondary Endpoint**: `POST /json/bookings.getHotelDescriptionPhotosV2` for hotel details and images
- **Integration Notes**: XML-based API requiring custom parsing, extensive property data available

### Data Models for Hotel Integration
[Source: architecture/data-models.md]
**HotelOption Entity Requirements**:
- **hotel_id**: UUID - Primary identifier for database storage
- **trip_id**: UUID - Foreign key relationship to Trip entity
- **name**: string - Hotel name for user display
- **price_per_night**: decimal - Nightly rate for budgeting and comparison
- **location**: Point - Geographic coordinates for proximity calculations
- **rating**: float - Hotel rating (1-5) for quality comparison
- **amenities**: JSON - Available amenities for user preference matching

### Database Schema Requirements
[Source: architecture/database-schema.md]
**Hotel Options Table Schema**:
```sql
CREATE TABLE hotel_options (
    hotel_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    trip_id UUID REFERENCES trips(trip_id) ON DELETE CASCADE,
    external_id VARCHAR(255), -- Booking.com/provider's ID
    name VARCHAR(255) NOT NULL,
    address TEXT,
    location POINT NOT NULL, -- PostGIS point for geographic queries
    price_per_night DECIMAL(10,2) NOT NULL,
    currency CHAR(3) DEFAULT 'USD',
    rating DECIMAL(2,1) CHECK (rating >= 1 AND rating <= 5),
    amenities JSONB DEFAULT '[]',
    photos JSONB DEFAULT '[]',
    booking_url TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

### Coding Standards Requirements
[Source: architecture/coding-standards.md]
**Python Naming Conventions**:
- **Classes**: PascalCase (`HotelAgent`, `BookingApiClient`)
- **Functions**: snake_case (`search_hotels`, `calculate_distance`)
- **API Endpoints**: kebab-case (`/api/v1/hotels/search-options`)
- **Database Tables**: snake_case (`hotel_options`, `external_id`)

**Critical Rules**:
- **Never log sensitive data**: No API keys, authentication tokens in logs - use structured logging with redaction
- **All external API calls must use circuit breakers**: Prevent cascade failures from Booking.com API outages
- **Database queries must use repository pattern**: Never direct ORM calls from agent classes
- **All async operations require timeout**: Prevent hanging requests from external services (30s default)

### API Response Standards
[Source: architecture/rest-api-spec.md]
**Hotel Search Response Format**:
```json
{
  "status": "success",
  "data": {
    "hotels": [
      {
        "hotel_id": "uuid",
        "name": "string",
        "price_per_night": "decimal",
        "location": {
          "latitude": "number",
          "longitude": "number"
        },
        "rating": "float",
        "amenities": ["array"]
      }
    ]
  },
  "error": null
}
```

## Testing

### Testing Standards
[Source: architecture/test-strategy-and-standards.md]
- **Framework**: pytest 7.4+ with async support for backend testing
- **Test Files**: `test_*.py` adjacent to source code in `tests/` directory structure
- **Coverage Target**: 90%+ for agent logic and business rules, 100% for external API integrations
- **Test Organization**:
  - `src/tests/unit/test_agents/` - Agent class unit testing with mocked dependencies
  - `src/tests/integration/test_services/` - API integration testing with Docker test containers
  - `src/tests/fixtures/` - JSON fixtures for API responses with realistic hotel data

**Hotel Agent-Specific Testing Requirements**:
- **Unit Tests**: Test all public methods with edge cases, mock all external dependencies including Redis and Booking.com API
- **Integration Tests**: Multi-component interactions with database and Redis using Testcontainers
- **API Mock Tests**: httpx-mock for stubbing Booking.com API with realistic hotel responses
- **Error Condition Tests**: Cover timeout scenarios, rate limit responses, and API failure modes
- **Performance Tests**: API response time validation <30s for hotel search operations

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-09-01 | 1.0 | Initial story creation with comprehensive hotel agent architecture context | Bob (Scrum Master) |

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-20250514

### Debug Log References
*To be populated by development agent*

### Completion Notes
- **Task 1 - Hotel Agent Base Implementation (AC1): COMPLETED**
  - Successfully implemented HotelAgent class inheriting from BaseAgent with proper dependency injection
  - Added hotel-specific configurations (cache TTL, max results, timeout settings)
  - Implemented comprehensive validation and error handling for search requests
  - Created 21 unit tests covering all functionality with 100% pass rate
  - Added hotel data models (HotelSearchRequest, HotelOption, HotelLocation, HotelSearchResponse, HotelComparisonResult) to external.py
  - All code follows project coding standards and passes linting checks

- **Task 2 - Booking.com API Integration Service (AC1, AC2): COMPLETED**
  - Successfully implemented BookingClient class with XML API integration for hotel search
  - Added proper authentication with username/password credentials 
  - Implemented hotel availability search using `/json/bookings.getHotelAvailabilityV2` endpoint
  - Added hotel details search using `/json/bookings.getHotelDescriptionPhotosV2` endpoint
  - Implemented rate limiting (100 requests/minute) with semaphore and timing controls
  - Added circuit breaker pattern for API resilience with 5-failure threshold and 5-minute recovery
  - Comprehensive XML response parsing with error handling for malformed data
  - Created 24 unit tests covering all functionality with 100% pass rate
  - All code follows project coding standards with proper error handling and logging

- **Task 3 - Hotel Data Models (AC3): COMPLETED**
  - Hotel data models already implemented in external.py during Task 1 (HotelSearchRequest, HotelOption, HotelLocation, HotelSearchResponse, HotelComparisonResult)
  - All models include proper type hints, validation, and match database schema requirements
  - HotelOption model supports amenities as JSONB field with list[str] type
  - Models include comprehensive field validation (coordinates limits, positive prices, rating limits, currency format, etc.)
  - Created 32 comprehensive unit tests covering all models with validation, serialization, and edge case testing
  - All tests pass with 100% coverage of model functionality
  - Code follows project coding standards and passes linting checks

- **Task 4 - Hotel Search Logic (AC2): COMPLETED**
  - Successfully implemented _search_hotels method in HotelAgent with full Booking.com API integration
  - Added comprehensive parameter handling: location, check-in/out dates, guest count, room count, and budget filters
  - Integrated with existing BookingClient service with proper error handling and timeout management
  - Implemented date format parsing supporting both string and datetime objects
  - Added budget filtering logic that applies after API response to filter out expensive options
  - Enhanced search metadata to include all search parameters, API response time, and error information
  - Created robust error handling that returns empty results with detailed error metadata on failures
  - Added performance tracking with search time measurement in milliseconds
  - Redis caching integration already handled by BaseAgent process method with cache key generation
  - Created 11 comprehensive unit tests covering all search functionality scenarios:
    * Successful search with API integration and result conversion
    * Budget filtering functionality with price comparison
    * Date parsing and format handling (string/datetime)
    * Invalid date format error handling
    * API error handling with graceful fallback
    * Malformed API response handling and validation
    * Maximum results limit enforcement
    * Complete metadata tracking including API response times
    * Result caching with Redis integration
    * Cached result retrieval and validation
    * Performance metrics tracking and logging
  - All tests pass with 100% success rate and proper mocking of external dependencies
  - Code follows project coding standards with proper type hints, error handling, and passes all linting checks
  - Integration maintains consistency with existing BaseAgent architecture and patterns

### File List
**New Files Created:**
- `packages/api/src/travel_companion/agents/hotel_agent.py` - HotelAgent implementation with hotel-specific configurations
- `packages/api/src/tests/test_agents/test_hotel_agent.py` - Comprehensive unit tests (21 tests)
- `packages/api/src/travel_companion/services/external_apis/booking.py` - Booking.com API client with XML integration
- `packages/api/src/tests/test_services/test_external_apis/test_booking.py` - Booking.com API client unit tests (24 tests)
- `packages/api/src/tests/test_models/test_hotel_models.py` - Hotel data models unit tests (32 tests)

**Modified Files:**
- `packages/api/src/travel_companion/models/external.py` - Added hotel data models (HotelSearchRequest, HotelOption, HotelLocation, HotelSearchResponse, HotelComparisonResult)
- `packages/api/src/tests/test_agents/test_hotel_agent.py` - Updated with comprehensive search functionality tests (11 new tests added to existing test suite)

## QA Results
*To be populated by QA agent*