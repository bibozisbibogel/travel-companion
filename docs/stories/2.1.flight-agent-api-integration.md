# <!-- Powered by BMAD™ Core -->

# Story 2.1: Flight Agent & API Integration

## Status
Draft

## Story
**As a** travel planner,
**I want** the system to fetch and compare flight options,
**so that** I can see available flights with pricing and scheduling information.

## Acceptance Criteria
1. Flight Agent class implemented with Amadeus API integration
2. Flight search functionality with origin, destination, dates, and passenger count
3. Flight data model with airline, price, duration, and schedule information
4. Error handling for API rate limits and unavailable routes
5. Flight comparison logic ranking by price, duration, and departure time
6. Mock flight data for development and testing environments

## Tasks / Subtasks
- [ ] Flight Agent Base Implementation (AC: 1)
  - [ ] Create base agent class in `packages/api/src/travel_companion/agents/base.py`
  - [ ] Implement FlightAgent class in `packages/api/src/travel_companion/agents/flight_agent.py`
  - [ ] Add agent initialization with configuration and dependency injection
  - [ ] Create unit tests for base agent functionality

- [ ] Amadeus API Integration Service (AC: 1, 2)
  - [ ] Create Amadeus API client in `packages/api/src/travel_companion/services/external_apis/amadeus.py`
  - [ ] Implement OAuth 2.0 authentication with client credentials flow
  - [ ] Add flight search endpoint integration with `/shopping/flight-offers`
  - [ ] Implement proper rate limit handling (10 requests/second, 1000/month)
  - [ ] Create unit tests for API client with httpx-mock

- [ ] Flight Data Models (AC: 3)
  - [ ] Create Pydantic models in `packages/api/src/travel_companion/models/external.py`
  - [ ] Implement FlightOption model matching database schema
  - [ ] Add FlightSearchRequest and FlightSearchResponse models
  - [ ] Create model validation with proper type hints
  - [ ] Add unit tests for model validation and serialization

- [ ] Flight Search Logic (AC: 2)
  - [ ] Implement search_flights method in FlightAgent
  - [ ] Add origin, destination, dates, and passenger count parameters
  - [ ] Integrate with Amadeus API service
  - [ ] Add result caching with Redis to optimize API usage
  - [ ] Create comprehensive unit tests for search functionality

- [ ] Error Handling and Resilience (AC: 4)
  - [ ] Implement circuit breaker pattern for external API calls
  - [ ] Add timeout configuration (30s default) for API requests
  - [ ] Handle API rate limit responses (429) with exponential backoff
  - [ ] Add fallback logic for unavailable routes
  - [ ] Create error handling tests with mock failures

- [ ] Flight Comparison and Ranking (AC: 5)
  - [ ] Implement flight ranking algorithm considering price, duration, departure time
  - [ ] Add weighted scoring system for flight options
  - [ ] Create flight filtering methods by budget and preferences
  - [ ] Add sorting and pagination for large result sets
  - [ ] Create unit tests for ranking and comparison logic

- [ ] Mock Data and Testing Environment (AC: 6)
  - [ ] Create realistic flight data fixtures in `tests/fixtures/`
  - [ ] Implement mock Amadeus API responses for testing
  - [ ] Add development mode flag to use mock data
  - [ ] Create comprehensive integration tests with test containers
  - [ ] Add performance tests for flight search operations

## Dev Notes

### Previous Story Insights
From Story 1.5 completion:
- Backend API Gateway fully operational with FastAPI framework
- Database connection established with Supabase PostgreSQL
- Redis caching layer configured and available
- JWT authentication system implemented
- Standardized API response format with status, data, and error fields
- Test infrastructure established with pytest and Docker containers

### Tech Stack Requirements
[Source: architecture/tech-stack.md]
- **Python**: 3.11+ for backend development with async support, LangGraph compatibility, rich AI/ML ecosystem
- **FastAPI**: 0.104+ for API orchestration with high performance, excellent OpenAPI docs, async native
- **httpx**: 0.25+ for async API calls - modern async HTTP client for Python
- **Pydantic**: 2.5+ for data models & validation with runtime type checking, excellent FastAPI integration
- **pytest**: 7.4+ for backend testing with industry standard, excellent fixture support
- **Redis**: 7.2+ for API caching & rate limiting with high performance, pub/sub for real-time updates

### Project Structure and File Locations
[Source: architecture/source-tree.md]
**Flight Agent Implementation Structure**:
- **Base Agent**: `packages/api/src/travel_companion/agents/base.py` (to be created)
- **Flight Agent**: `packages/api/src/travel_companion/agents/flight_agent.py` (to be created)
- **Amadeus API Client**: `packages/api/src/travel_companion/services/external_apis/amadeus.py` (to be created)
- **External API Models**: `packages/api/src/travel_companion/models/external.py` (to be created)
- **Agent Tests**: `packages/api/src/tests/test_agents/test_flight_agent.py` (to be created)
- **API Client Tests**: `packages/api/src/tests/test_services/test_amadeus.py` (to be created)
- **Test Fixtures**: `packages/api/src/tests/fixtures/flight_data.json` (to be created)

### Amadeus API Integration Requirements
[Source: architecture/external-apis.md]
**Amadeus Travel API Configuration**:
- **Base URL**: `https://api.amadeus.com/v2/`
- **Authentication**: OAuth 2.0 with client credentials flow
- **Rate Limits**: 10 requests/second, 1000 requests/month (sandbox environment)
- **Primary Endpoint**: `GET /shopping/flight-offers` for flight search with flexible parameters
- **Integration Notes**: Primary flight data source, requires careful rate limit management and caching strategy

### Data Models for Flight Integration
[Source: architecture/data-models.md]
**FlightOption Entity Requirements**:
- **flight_id**: UUID - Primary identifier for database storage
- **trip_id**: UUID - Foreign key relationship to Trip entity
- **airline**: string - Airline name for user display
- **price**: decimal - Flight price for comparison and budgeting
- **departure_time**: datetime - Departure timestamp for scheduling
- **arrival_time**: datetime - Arrival timestamp for scheduling
- **duration**: integer - Flight duration in minutes for comparison
- **stops**: integer - Number of stops for user preference filtering

### Database Schema Requirements
[Source: architecture/database-schema.md]
**Flight Options Table Schema**:
```sql
CREATE TABLE flight_options (
    flight_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    trip_id UUID REFERENCES trips(trip_id) ON DELETE CASCADE,
    external_id VARCHAR(255), -- Amadeus API provider's ID
    airline VARCHAR(100) NOT NULL,
    flight_number VARCHAR(20),
    origin VARCHAR(10) NOT NULL, -- Airport code
    destination VARCHAR(10) NOT NULL,
    departure_time TIMESTAMP WITH TIME ZONE NOT NULL,
    arrival_time TIMESTAMP WITH TIME ZONE NOT NULL,
    duration_minutes INTEGER NOT NULL,
    stops INTEGER DEFAULT 0,
    price DECIMAL(10,2) NOT NULL,
    currency CHAR(3) DEFAULT 'USD',
    booking_url TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

### Coding Standards Requirements
[Source: architecture/coding-standards.md]
**Python Naming Conventions**:
- **Classes**: PascalCase (`FlightAgent`, `AmadeusClient`)
- **Functions**: snake_case (`search_flights`, `calculate_duration`)
- **API Endpoints**: kebab-case (`/api/v1/flights/search-options`)
- **Database Tables**: snake_case (`flight_options`, `external_id`)

**Critical Rules**:
- **Never log sensitive data**: No API keys, authentication tokens in logs - use structured logging with redaction
- **All external API calls must use circuit breakers**: Prevent cascade failures from Amadeus API outages
- **Database queries must use repository pattern**: Never direct ORM calls from agent classes
- **All async operations require timeout**: Prevent hanging requests from external services (30s default)

### API Response Standards
[Source: architecture/rest-api-spec.md]
**Flight Search Response Format**:
```json
{
  "status": "success",
  "data": {
    "flights": [
      {
        "flight_id": "uuid",
        "airline": "string", 
        "price": "decimal",
        "departure_time": "datetime",
        "arrival_time": "datetime",
        "duration": "integer"
      }
    ]
  },
  "error": null
}
```

## Testing

### Testing Standards
[Source: architecture/test-strategy-and-standards.md]
- **Framework**: pytest 7.4+ with async support for backend testing
- **Test Files**: `test_*.py` adjacent to source code in `tests/` directory structure
- **Coverage Target**: 90%+ for agent logic and business rules, 100% for external API integrations
- **Test Organization**:
  - `src/tests/unit/test_agents/` - Agent class unit testing with mocked dependencies
  - `src/tests/integration/test_services/` - API integration testing with Docker test containers
  - `src/tests/fixtures/` - JSON fixtures for API responses with realistic flight data

**Flight Agent-Specific Testing Requirements**:
- **Unit Tests**: Test all public methods with edge cases, mock all external dependencies including Redis and Amadeus API
- **Integration Tests**: Multi-component interactions with database and Redis using Testcontainers
- **API Mock Tests**: WireMock for stubbing Amadeus API with realistic flight responses
- **Error Condition Tests**: Cover timeout scenarios, rate limit responses, and API failure modes
- **Performance Tests**: API response time validation <30s for flight search operations

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-09-01 | 1.0 | Initial story creation with comprehensive flight agent architecture context | Bob (Scrum Master) |

## Dev Agent Record

### Agent Model Used
*To be populated by development agent*

### Debug Log References
*To be populated by development agent*

### Completion Notes
*To be populated by development agent*

### File List
*To be populated by development agent*

## QA Results
*To be populated by QA agent*